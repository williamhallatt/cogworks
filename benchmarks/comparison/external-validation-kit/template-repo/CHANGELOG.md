# Changelog

## 2026-03-03
- Initial external-validation benchmark template.
- Added protocol runner, contamination scanner, reproducibility verifier, and trust report.
- Added true skill-installed execution mode with `npx skills add` for Codex.
- Added capability split (`skill_installed` vs `protocol_prompt`) and mode-split reporting.
- Added install/use evidence gates: `skill-install-report.json` and `skill-use-evidence.json`.
