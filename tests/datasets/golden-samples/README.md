# Golden Sample Warning

This directory contains expected outputs and control fixtures for tests.

It is not a canonical skill source.

Default retrieval policy:

- Do not load `tests/datasets/golden-samples/` by default.
- Treat any `SKILL.md` here as a test expectation, not a live skill.
