# Copilot Native-Subagents Smoke Example

This directory preserves one minimized example artifact set from a completed Copilot CLI agentic smoke run.

Use it for:
- contract inspection
- validator regression checks
- provenance review for the documented Copilot smoke proof

Do not treat this directory as default retrieval context for ordinary work.
The `source-trust-gate.json` file is a small compatibility backfill so the preserved example validates against the current runtime contract.

Contents:
- `run-root/` — preserved agentic run artifacts
- `skill-output/` — preserved generated skill output
