# Charter Review: Lambert + Hudson Post-Spec-Alignment

**Date:** 2026-03-04 12:30:49Z  
**Context:** Spec alignment review for TD-017/TD-018 (compatibility gates) and D-022 (quality measurement dependency)

## Lambert Updates (TD-017/TD-018)

Updated D6 responsibility to explicitly enumerate all Claude Code-specific fields requiring `compatibility:` frontmatter gate:
- `$ARGUMENTS` / `$N` interpolation
- `disable-model-invocation: true`
- `user-invocable: false`
- `context: fork`

Clarified that Lambert must validate any generated skills using these fields include the gate in frontmatter.

## Hudson Updates (D-022 Dependency)

Clarified Layer 1 vs Layer 2 distinction:
- **Layer 1 (Deterministic):** `scripts/validate-quality-gates.sh` covers structural/deterministic checks. Fully in Hudson's scope with no dependencies.
- **Layer 2 (Behavioral):** Behavioral trace validity testing is a sequenced dependency on D-022 — Parker must define `quality_score` measurement validity first before Hudson implements the harness.

Made D-022 dependency explicit in D8 responsibility to prevent circular waiting.

## Decision Inbox

Empty — no pending inbox files to merge.
